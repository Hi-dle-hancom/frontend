extends layout

block content
  div.error-container
    div.error-card
      div.error-status
        span #{error.status || 500}
      h1.error-title 오류가 발생했습니다
      p.error-message #{message}
      a.btn-primary(href="/users") 
        span.material-symbols-rounded home
        | 홈으로 돌아가기

  style.
    .error-container {
      display: flex;
      align-items: center;
      justify-content: center;
      min-height: 70vh;
    }
    
    .error-card {
      background: rgba(255, 255, 255, 0.03);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      padding: 40px;
      text-align: center;
      max-width: 500px;
      width: 100%;
      box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.1);
      position: relative;
      overflow: hidden;
      backdrop-filter: blur(10px);
    }
    
    .error-card::before {
      content: '';
      position: absolute;
      top: -80%;
      left: -100%;
      width: 300%;
      height: 200%;
      background: linear-gradient(
        to bottom right,
        rgba(var(--primary-rgb), 0.03) 0%,
        rgba(var(--secondary-rgb), 0.03) 100%
      );
      transform: rotate(35deg);
      z-index: -1;
      pointer-events: none;
    }
    
    .error-status {
      font-size: 92px;
      font-weight: 700;
      margin-bottom: 20px;
      line-height: 1;
      background: linear-gradient(to right, var(--primary), var(--secondary));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      display: inline-block;
    }
    
    .error-title {
      color: var(--text);
      font-size: 28px;
      margin-bottom: 15px;
    }
    
    .error-message {
      color: var(--text-secondary);
      margin-bottom: 30px;
      font-size: 16px;
      line-height: 1.6;
    }
    
    .btn-primary {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
      padding: 12px 24px;
      margin: 0 auto;
    }
    
    .material-symbols-rounded {
      font-size: 18px;
    } 