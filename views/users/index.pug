extends ../layout

block content
  h2.section-title 사용자 목록
  
  p.add-user-container
    a(href="/users/new" class="btn-new")
      span.material-symbols-rounded add
      | 새 사용자 추가
  
  table
    thead
      tr
        th ID
        th 이름
        th 나이
        th 결혼 여부
        th 액션
    tbody
      if users && users.length > 0
        each user in users
          tr
            td= user.id
            td= user.name
            td= user.age
            td
              span(class=user.married ? 'tag married' : 'tag single')= user.married ? 'Yes' : 'No'
            td.actions
              a(href=`/users/${user.id}/edit`)
                span.material-symbols-rounded edit
                span 수정
              form(action=`/users/${user.id}?_method=DELETE`, method="post", class="delete-form")
                button.delete-btn(type="submit")
                  span.material-symbols-rounded delete
                  span 삭제
      else
        tr.no-data
          td(colspan="5") 등록된 사용자가 없습니다.

  style.
    .add-user-container {
      display: flex;
      justify-content: flex-end;
      margin-bottom: 20px;
    }
    
    .btn-new .material-symbols-rounded {
      margin-right: 6px;
      font-size: 18px;
    }
    
    .tag {
      display: inline-block;
      padding: 4px 10px;
      border-radius: 12px;
      font-size: 12px;
      font-weight: 500;
    }
    
    .tag.married {
      background-color: rgba(94, 96, 206, 0.15);
      color: #5e60ce;
    }
    
    .tag.single {
      background-color: rgba(128, 128, 128, 0.15);
      color: #808080;
    }
    
    .actions {
      display: flex;
      gap: 15px;
    }
    
    .actions a {
      display: flex;
      align-items: center;
      margin-right: 0;
    }
    
    .actions .material-symbols-rounded {
      font-size: 18px;
      margin-right: 4px;
    }
    
    .delete-form {
      margin: 0;
      padding: 0;
      background: none;
      border: none;
      box-shadow: none;
      display: inline;
    }
    
    .delete-btn {
      color: var(--secondary);
      background: none;
      border: none;
      padding: 0;
      font: inherit;
      cursor: pointer;
      display: flex;
      align-items: center;
    }
    
    .delete-btn:hover {
      color: var(--secondary-dark);
    }
    
    .delete-btn:hover::after {
      background-color: var(--secondary-dark);
    }
    
    .no-data {
      text-align: center;
      color: var(--text-secondary);
      font-style: italic;
    } 