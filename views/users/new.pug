extends ../layout

block content
  h2.section-title 새 사용자 등록
  
  form(action="/users", method="post")
    div.form-group
      label(for="name") 이름
      input#name(type="text", name="name", required, placeholder="이름을 입력하세요")
    
    div.form-group
      label(for="age") 나이
      input#age(type="number", name="age", required, min="1", placeholder="나이를 입력하세요")
    
    div.form-group.checkbox-group
      label(for="married") 결혼 여부
      div.checkbox-wrapper
        input#married(type="checkbox", name="married")
        span.checkbox-label 결혼
    
    div.buttons
      button.btn-primary(type="submit")
        span.material-symbols-rounded save
        | 등록
      a.btn-secondary(href="/users")
        span.material-symbols-rounded arrow_back
        | 취소

  style.
    .checkbox-group {
      display: flex;
      flex-direction: column;
    }
    
    .checkbox-wrapper {
      display: flex;
      align-items: center;
      margin-top: 8px;
    }
    
    .checkbox-label {
      margin-left: 8px;
      color: var(--text);
      font-size: 15px;
    }
    
    .btn-primary, .btn-secondary {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
    }
    
    .btn-primary .material-symbols-rounded,
    .btn-secondary .material-symbols-rounded {
      font-size: 18px;
    }
    
    .btn-secondary {
      background-color: transparent;
      border: 1px solid var(--border);
      color: var(--text);
      box-shadow: none;
      transition: var(--transition);
      padding: 13px 24px;
      text-decoration: none;
    }
    
    .btn-secondary:hover {
      background-color: rgba(255, 255, 255, 0.05);
      transform: translateY(-3px);
    } 