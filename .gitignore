# Python
__pycache__/
*.pyc
*.pyo
*.pyd

# Byte-code files
*.py[cod]

# IDEs
.vscode/ # VS Code 설정 폴더 (선택 사항, 공유하고 싶다면 삭제)
.idea/ # IntelliJ IDEA 설정 폴더

# OS generated files
.DS_Store # macOS
Thumbs.db # Windows

# Conda environment directory
# Conda는 기본적으로 프로젝트 폴더 내에 가상 환경을 만들지 않지만,
# 만약 프로젝트 폴더 내 특정 위치에 가상 환경을 만들었다면 해당 폴더를 지정해 주세요.
# 예: 프로젝트 루트에 'venv'나 'env' 폴더를 만들었다면 아래와 같이 추가
# venv/
# env/

# 패키지 설치 파일 (선택 사항: 커밋할 수도 있고 안 할 수도 있음)
# requirements.txt # 이미 pip freeze로 생성했으니 필요 없으면 주석 처리하거나 삭제

# 기타
.env # 환경 변수 파일 (민감 정보 포함 가능성 높음)
*.sqlite3 # SQLite DB 파일 (민감 정보 포함 가능성 높음)